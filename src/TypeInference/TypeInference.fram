
import open Env
import TypeChecker

let ~env = Env { varMap = StrMap.empty, level = 0 }

let ~allowUnbound = False

pub let tr_prog expr = handle ~addt with TypeChecker.hAddt in TypeChecker.inferType expr